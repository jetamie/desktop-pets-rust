{
  "name": "desktop-pets-rust",
  "version": "1.2.1",
  "description": "Tauri 桌面宠物应用",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:frontend": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "build": "tauri build",
    "build:frontend": "tsc && vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["desktop", "pet", "tauri"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-legacy": "^5.4.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
